spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medical-file?serverTimezone=UTC
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true

server:
  servlet:
    context-path: /medical-file
  port: 8086